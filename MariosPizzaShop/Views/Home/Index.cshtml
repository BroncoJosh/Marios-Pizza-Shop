@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Pizza's of the Week</h2>
<h4>Our Weekly Selection - Just for You!</h4>

<div class="row">
    @foreach (var pizza in Model.PizzasOfTheWeek)
    {
        @Html.Partial("PizzasOfTheWeekSummary", pizza)
    }
</div>